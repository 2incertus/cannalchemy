export default function StrainDetail() {
  return (
    <div className="page flex items-center justify-center">
      <p style={{ color: "var(--cream-dim)" }}>Strain Detail — coming soon</p>
    </div>
  );
}
